
<salon-map
        lat="{{ $contact->first()->lat }}"
        long="{{ $contact->first()->long }}"
        second-lat="{{ $contact[1]->lat }}"
        second-long="{{ $contact[1]->long }}"
></salon-map>
